# Bloom Filters

- A Bloom Filter is a space-efficient probabilistic data structure used to test whether an element may exist in a set
- It can produce false positives but never false negatives
- It is commonly used in databases, caches, and distributed systems to reduce unnecessary lookups