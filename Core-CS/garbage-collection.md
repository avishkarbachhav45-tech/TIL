# Garbage Collection

- Garbage Collection automatically identifies and removes objects that are no longer reachable by a program
- It helps manage memory without requiring developers to manually free unused objects
- The garbage collector reclaims memory that can no longer be used by the application
- Java uses Garbage Collection to manage objects stored in the heap