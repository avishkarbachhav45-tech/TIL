# Middleware

- Middleware executes between receiving a request and sending a response
- It can modify requests, responses, or perform additional processing
- Common uses include authentication, logging, and error handling