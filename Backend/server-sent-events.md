# Server-Sent Events (SSE)

- Server-Sent Events allow a server to push real-time updates to the client over HTTP
- SSE supports one-way communication from the server to the browser
- It is commonly used for live notifications, dashboards, and news feeds