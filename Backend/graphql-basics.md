# GraphQL Basics

- GraphQL is an API query language that lets clients request exactly the data they need
- A GraphQL API typically exposes a single endpoint instead of many resource-specific endpoints
- Queries retrieve data, mutations modify data, and subscriptions support real-time updates