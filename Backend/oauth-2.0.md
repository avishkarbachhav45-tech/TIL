# OAuth 2.0

- OAuth 2.0 is an authorization framework that allows secure access without sharing passwords
- It uses access tokens to grant limited access to protected resources
- It is commonly used for "Login with Google" and "Login with GitHub"