# Kubernetes Deployments

- A Kubernetes Deployment manages and maintains a desired number of Pod replicas
- It automatically creates new Pods when existing Pods fail
- Deployments support rolling updates when a new application version is released
- They also allow rolling back to a previous version when an update causes problems