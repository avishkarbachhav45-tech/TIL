# Kubernetes Services

- A Kubernetes Service provides a stable network endpoint for accessing Pods
- It allows traffic to reach the correct Pods even when Pod IP addresses change
- Common Service types include ClusterIP, NodePort, and LoadBalancer