# Kubernetes ConfigMaps

- A ConfigMap stores non-sensitive configuration data separately from application code
- Pods can consume ConfigMap values as environment variables or mounted files
- ConfigMaps make it easier to change application configuration without rebuilding container images