# Kubernetes Secrets

- Kubernetes Secrets store sensitive data such as passwords, API keys, and tokens
- Secrets can be exposed to Pods as environment variables or mounted files
- Secrets separate sensitive configuration from application code and container images