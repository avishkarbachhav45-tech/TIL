# Docker Compose

- Docker Compose manages multiple containers using a single configuration file
- Services are defined in a docker-compose.yml file
- It simplifies running applications with databases and other dependencies