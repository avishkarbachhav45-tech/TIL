# Kubernetes RBAC

- RBAC (Role-Based Access Control) controls what users and services can do in a Kubernetes cluster
- Roles define permissions, while RoleBindings assign those permissions to users or service accounts
- RBAC follows the principle of least privilege by granting only the required permissions