# Docker Volumes

- Docker Volumes are used to persist data generated by containers
- Volumes exist independently from the container lifecycle
- They prevent important data from being lost when a container is removed
- Volumes are commonly used for databases and other stateful applications