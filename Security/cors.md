# CORS

- CORS (Cross-Origin Resource Sharing) controls whether a browser can make requests to a different origin
- Servers use CORS response headers to specify which origins, methods, and headers are allowed
- CORS is enforced by browsers and is not a replacement for authentication or authorization