# SQL Injection

- SQL Injection occurs when untrusted user input is directly included in SQL queries
- Attackers can manipulate the query to access, modify, or delete database data
- Parameterized queries and prepared statements help prevent SQL Injection