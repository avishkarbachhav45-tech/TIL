# Password Hashing

- Password hashing converts passwords into one-way values before storing them in a database
- Hashes are designed to make it difficult to recover the original password
- Passwords should be hashed using algorithms like bcrypt, scrypt, or Argon2
- A unique salt is added to each password to protect against common attacks