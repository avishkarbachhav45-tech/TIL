# HTTP Strict Transport Security (HSTS)

- HSTS tells browsers to access a website only through HTTPS
- It helps prevent attackers from forcing users to connect through insecure HTTP
- HSTS is enabled using the `Strict-Transport-Security` response header
- It can also include options such as `max-age`, `includeSubDomains`, and `preload`