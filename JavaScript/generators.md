# JavaScript Generators

- Generators are special functions that can pause and resume their execution
- A generator function is declared using the `function*` syntax
- The `yield` keyword pauses execution and returns a value
- Generators are useful for creating iterators and controlling step-by-step execution