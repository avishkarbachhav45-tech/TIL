# JavaScript Closures

- A closure allows a function to access variables from its outer scope
- Closures preserve data even after outer function execution
- Commonly used in callbacks and data encapsulation