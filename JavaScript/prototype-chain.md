# Prototype Chain

- JavaScript objects can inherit properties and methods from other objects
- The prototype chain is used to resolve property and method lookups
- The lookup continues until the property is found or the chain ends with null