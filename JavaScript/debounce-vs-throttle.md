# Debounce vs Throttle

- Debounce delays function execution until events stop occurring for a specified time
- Throttle limits how frequently a function can execute during continuous events
- Both techniques improve performance in event-heavy applications