# JavaScript Promises

- A Promise represents the eventual result of an asynchronous operation
- A Promise can be pending, fulfilled, or rejected
- then(), catch(), and finally() are used to handle Promise outcomes