# Call, Apply and Bind

- call() invokes a function with a specified this value and arguments
- apply() is similar to call() but accepts as an array
- bind() returns a new function with this permanently bound