# Linux Namespaces

- Linux namespaces isolate system resources so processes can have separate views of the system
- Different namespaces can isolate processes, network interfaces, mounts, users, and other resources
- Containers use Linux namespaces as one of the core mechanisms for process isolation