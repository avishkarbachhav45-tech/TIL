# Linux Control Groups (cgroups)

- cgroups limit and monitor resource usage of processes
- They can control CPU, memory, disk I/O, and other resources
- Containers use cgroups to prevent one container from consuming all available system resources