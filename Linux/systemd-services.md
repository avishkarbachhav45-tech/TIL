# Systemd Services

- Systemd is the default service manager in most Linux distributions
- It starts, stops, and manages system services during boot and runtime
- Common commands include systemctl start, stop, enable, and status