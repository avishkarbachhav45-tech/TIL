# Linux Shell

- The shell is a command-line interface for interacting with the operating system
- It accepts commands from users and executes them
- Bash is one of the most commonly used Linux shells