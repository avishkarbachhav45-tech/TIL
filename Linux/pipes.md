# Linux Pipes

- A pipe `|` passes the output of one command as the input to another command
- Pipes allow multiple Linux commands to be combined into a single command workflow
- They are useful for filtering and processing command output efficiently
- Example: `ps aux | grep java` filters running processes to find Java-related entries