# Linux iptables

- iptables is a Linux firewall utility used to control incoming and outgoing network traffic
- It filters packets using rules based on IP addresses, ports, protocols, and interfaces
- Rules can allow, reject, or drop network traffic
- It is commonly used to improve server security by restricting unwanted connections