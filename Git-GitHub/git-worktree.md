# Git Worktree

- Git Worktree allows us to work on multiple branches of the same repository at the same time
- Each worktree has its own working directory while sharing the same Git history
- It helps avoid repeatedly switching between branches
- It is useful when working on a feature while fixing another branch