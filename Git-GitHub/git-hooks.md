# Git Hooks

- Git Hooks are scripts that run automatically before or after Git events
- They help automate tasks such as testing, formatting, and commit validation
- Common hooks include pre-commit, commit-msg, and pre-push