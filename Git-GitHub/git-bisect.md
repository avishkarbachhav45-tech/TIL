# Git Bisect

- Git Bisect uses binary search to find the commit that introduced a bug
- It repeatedly divides the commit history into smaller ranges
- It requires marking commits as good or bad until the problematic commit is identified