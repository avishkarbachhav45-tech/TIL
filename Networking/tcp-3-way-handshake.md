# TCP 3-Way Handshake

- TCP establishes a reliable connection using a three-step handshake
- The client sends SYN, the server responds with SYN-ACK, and the client sends ACK
- After the handshake, both sides can exchange data reliably
