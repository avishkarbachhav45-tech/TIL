# Cloud DNS

- Cloud DNS is a managed service that translates domain names into IP addresses
- It removes the need to maintain DNS infrastructure manually
- DNS records can route users to different cloud resources such as load balancers, servers, or applications
- Cloud DNS services provide features such as health checks, routing policies, and high availability