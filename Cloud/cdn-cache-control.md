# CDN Cache Control

- Cache-Control headers tell browsers and CDNs how a resource should be cached
- max-age defines how long a response can remain fresh
- public and private control whether shared caches can store a response
- no-cache requires the cache to revalidate the response before using it